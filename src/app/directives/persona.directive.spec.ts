import { PersonaDirective } from './persona.directive';

describe('PersonaDirective', () => {
  it('should create an instance', () => {
    const directive = new PersonaDirective();
    expect(directive).toBeTruthy();
  });
});
